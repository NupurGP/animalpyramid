export class Animal {
  name: string;
  pyramidLevel: number;
  imagePath: string;

  constructor(name: string, pyramidLevel: number, imagePath: string) {
    this.name = name;
    this.pyramidLevel = pyramidLevel;
    this.imagePath = imagePath;
  }
}
